import 'package:flutter/cupertino.dart';

class ClassUser extends StatelessWidget {
  const ClassUser({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
